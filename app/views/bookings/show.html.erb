<%= link_to '<i class="material-icon right"></i>Logout'.html_safe, logout_path(@user), class: "waves-effect waves-light btn" %>

<div class="row">
	<div class="col s6 m6">
	  <div class="card blue-grey darken-1">
	    <div class="card-content white-text">
	      <span class="card-title">User: <%= @booking.username %></span>
	      <span class="card-title">Room: <%= @booking.name %></span>
	      <p>start_time: <%= @booking.start_time.utc %></p>
	      <p>end_time: <%= @booking.end_time.utc %></p>
	      <p>Status: <%= @booking.status %></p>
	    </div>
	  </div>
	  <% if @booking.username == current_user.name %>
	  	<%= link_to '<i class="material-icon right"></i>Edit'.html_safe, edit_booking_path(@booking,:room_id => params[:room_id],:name => @booking.name), class: "waves-effect waves-light btn" %>
	  	<%= link_to 'Delete', @booking, method: :delete, data: { confirm: 'Are you sure want to delete?'}, class: "waves-effect waves-light btn" %>
	  <% end %>
	  <%= link_to '<i class="material-icon right"></i>Rooms'.html_safe, rooms_path, class: "waves-effect waves-light btn" %>
	</div>
</div>




